---
# 監査レポート

**対象タスク**: 弁護人AI機能のゲストユーザー開放  
**対象ブランチ**: feature/20260525-091051  
**実施日**: 2026-05-25  

## サマリー
- 指摘件数: HIGH 0件 / MEDIUM 1件 / LOW 0件

---

## 指摘一覧

### [MEDIUM-001] `verifyGuestToken` の例外が未処理（`defense/route.ts:29`, `draft/route.ts:41`）

- **内容**:  
  既存の `argument/route.ts:26-48` および `route.ts:25-48` では、`verifyGuestToken` 呼び出しを含む認証ブロック全体を try-catch で囲み、例外時に `{ error: "サーバー設定エラーが発生しました。管理者に連絡してください。", status: 500 }` を返している。  
  今回新たに実装された `defense/route.ts` の `resolveAuth`（29行目）と `draft/route.ts` のインライン認証ブロック（41行目）には try-catch がない。  
  `verifyGuestToken` は内部で `computeToken` を呼び出し、`GUEST_TOKEN_SECRET` が未設定の場合は `throw new Error("GUEST_TOKEN_SECRET is not set")` を投げる（`lib/guest-token.ts:4-6`）。この例外は未処理のまま Next.js のグローバルエラーハンドラに到達し、500 が返る。

- **影響**:  
  `GUEST_TOKEN_SECRET` が設定されていれば発生しない。ただし、設定漏れのまま本番デプロイされた場合、ゲストが弁護人AI エンドポイントにアクセスするたびに未処理例外が発生し、エラーの原因特定が困難になる。既存エンドポイントとのエラーハンドリングの不一致でもある。

- **修正案**:  
  `defense/route.ts` の `resolveAuth` 内、および `draft/route.ts` のゲストトークン検証ブロックを try-catch で囲み、既存の `argument/route.ts` と同様のエラーレスポンスを返す。

  ```ts
  // defense/route.ts の resolveAuth 内（ゲストパス）
  if (c.defendant_guest_name) {
    try {
      const cookieToken = req.cookies.get(`guest_defendant_${id}`)?.value;
      if (cookieToken && verifyGuestToken(id, cookieToken)) {
        return { user: null, userId: null, c, userRole: "defendant" as const, admin } as const;
      }
    } catch (err) {
      console.error("verifyGuestToken failed:", err);
      return { error: "サーバー設定エラーが発生しました。管理者に連絡してください。", status: 500 } as const;
    }
  }
  ```

  `draft/route.ts` の `else if (c.defendant_guest_name)` ブロックも同様に try-catch で囲む。

---

## 総評

認可フローの設計・実装は設計書の仕様に忠実で、全体として堅実。ゲストトークンの HMAC-SHA256 + `timingSafeEqual` による検証、`user_id IS NULL` でのゲスト識別と `case_id` による絞り込み、認証済みユーザーとゲストのデータ分離クエリ、Cookie の `httpOnly / secure / sameSite=strict / path=/api/cases/${id}` スコープ制限はいずれも適切に機能している。指摘は既存パターンとの一貫性欠如に留まる。

**判定: 通過**（HIGH 0件、指摘総数 1件 ≤ 5件）
