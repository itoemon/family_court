# 監査レポート

**対象コミット**: a83e17b（feature/20260520-084151）  
**監査日**: 2026-05-20  
**タスク**: HIGH-001 発言エンドポイント認可バイパスの修正

---

## サマリー

- 指摘件数: HIGH 0件 / MEDIUM 2件 / LOW 1件
- **判定: 通過**（HIGH 0件、指摘総数 3件 ≤ 5件）

HIGH-001 の修正自体は設計書通りに正しく実装されている。`body.role` の廃止・`callerRole` のサーバー側導出・`getUser()` によるセッション検証・`timingSafeEqual` によるタイミング攻撃対策、すべて問題なし。指摘は新規導入したゲストトークン機構の運用・設計上の懸念に限定される。

---

## 指摘一覧

### [MEDIUM-001] GUEST_TOKEN_SECRET 未設定時のゲスト機能全停止（lib/guest-token.ts:4）

- **内容**: `createHmac("sha256", process.env.GUEST_TOKEN_SECRET!)` は非 null アサーション（`!`）を使用しており、起動時・呼び出し時のいずれでも環境変数の存在チェックを行わない。`GUEST_TOKEN_SECRET` が未設定の場合、`createHmac` に `undefined` が渡り TypeError が発生する。この例外はキャッチされないため、ゲスト参加（`PATCH /api/cases/[id]`）・ゲスト発言（`POST /api/cases/[id]/argument`）のリクエストがすべて 500 Internal Server Error になる。エラーレスポンスにスタックトレースが含まれる場合、内部パスが漏洩する恐れもある。ビルドエラーにならないため、CI が通っても本番で初めて顕在化する。
- **修正案**: アプリ起動時（例: `lib/guest-token.ts` のモジュールトップレベル）で `if (!process.env.GUEST_TOKEN_SECRET) throw new Error("GUEST_TOKEN_SECRET is not set")` として起動失敗に昇格させる。もしくは `computeToken` 内で明示的にチェックして 500 の代わりに 503 を返す構造にする。`!` アサーションの除去も合わせて行うこと。

---

### [MEDIUM-002] HMAC トークンが決定論的で個別取り消し不可（lib/guest-token.ts:3–6）

- **内容**: `computeToken` の HMAC 入力は `"${caseId}:defendant"` のみであり、ランダム要素・タイムスタンプを含まない。これにより以下の性質を持つ。
  1. **同一ケースの token は常に同じ値**: Cookie が何らかの経路（開発環境での非 HTTPS 通信、サーバーサイドアクセスログ等）でキャプチャされた場合、Max-Age の 7 日間は完全に再利用可能。
  2. **個別セッションの取り消し不可**: 特定ゲストの Cookie を無効化するには `GUEST_TOKEN_SECRET` 全体をローテーションするしかなく、全ケース・全ゲストのセッションが同時に失効する。
  3. **セッションの監査証跡なし**: 同じ token 値が複数リクエストで使われても区別できない。

  現状の Cookie 属性（`httpOnly` / `SameSite=Strict` / 本番 `Secure`）により現実的な攻撃ベクターは限定的だが、運用上の flexibility がゼロであることは設計上の制約として認識すべきである。

- **修正案**: 短期的には現状容認（設計書が明示的にこのアプローチを選択している）。中長期的には、ゲスト参加時にランダムな nonce（`crypto.randomBytes(32).toString("hex")`）を DB の `cases` テーブルに保存し、HMAC 入力に含めることで個別取り消しと監査証跡を実現する。ゲスト再参加フロー実装時に合わせて検討すること。

---

### [LOW-001] validateApiKey がエラー種別を区別しない（lib/claude.ts:17）

- **内容**: `validateApiKey` は Anthropic API への実際のリクエストを送り、あらゆる例外を `catch {}` で握りつぶして `false` を返す（`lib/claude.ts:17`）。Anthropic 側の一時障害・ネットワークエラー・タイムアウトと、実際に無効なキーが同じ `false` として返るため、ユーザーには「APIキーが無効です」と表示されることになる。正常なキーを持つユーザーが Anthropic 障害時に登録できない問題が発生し、ユーザーがサポートに連絡する事態になりえる。セキュリティ上の直接的な脅威はないが、誤情報の表示はユーザー信頼に影響する。
- **修正案**: Anthropic SDK の `AuthenticationError`（ステータス 401/403）のみキャッチして `false` を返し、それ以外の例外は上位に再 throw するか、エラー種別（`"invalid_key"` / `"api_error"`）を返す union 型に変更する。

---

## 総評

HIGH-001 の修正は設計書の仕様を正確に実装している。`body.role` の廃止・DB 照合によるロール導出・`timingSafeEqual` の適用・Cookie の全セキュリティ属性、いずれも問題なし。MEDIUM-001 は環境変数の運用管理で対処可能であり、MEDIUM-002 はゲスト再参加フロー実装時に合わせて解消することが現実的である。
