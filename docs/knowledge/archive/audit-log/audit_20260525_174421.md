# 監査ログ: 2026-05-25 17:44

## 対象ブランチ

feature/20260525-131143

## 監査対象

| タスク | ファイル | 内容 |
|--------|----------|------|
| E-1 | `lib/defense.ts` | defenseHistory の truncate 適用 |
| E-2 | `app/api/cases/[id]/route.ts` | PATCH 非 asGuest パスへの try-catch 追加 |
| E-4 | `lib/claude.ts` | validateApiKey のエラー種別区別 |
| E-6 | `middleware.ts` | 保護パスのプレフィックスマッチ実装 |

## 監査結果サマリー

| 重要度 | 件数 |
|--------|------|
| HIGH   | 0    |
| MEDIUM | 0    |
| LOW    | 0    |
| 合格   | 10   |

---

## 各項目の確認結果

---

### E-1: `lib/defense.ts` — defenseHistory の truncate 適用

| チェック項目 | 結果 |
|---|---|
| `import { truncate, escapeXml }` が L2 に存在するか | 合格 |
| `generateDraft` の `defenseHistory` ループ（L79）に `escapeXml(truncate(m.content, 500))` が適用されているか | 合格 |
| truncate → escapeXml の順序が正しいか（truncate が内側） | 合格 |
| `generateDefenseResponse` の `dialogHistory.map`（L39）が `escapeXml(truncate(a.content, 500))` であるか | 合格 |
| `generateDraft` の `dialogHistory.map`（L73）が `escapeXml(truncate(a.content, 500))` であるか | 合格 |
| 対応が両関数で統一されているか | 合格 |

**詳細**:

- L2: `import { truncate, escapeXml } from "@/lib/text-utils"` — インポート確認済み。
- L79: `${m.role === "user" ? "あなた" : "弁護人AI"}: ${escapeXml(truncate(m.content, 500))}` — E-1 の対象箇所。`truncate(m.content, 500)` の出力を `escapeXml` が受け取る構造。truncate → escapeXml の順序正しい。
- D-1 監査で LOW-1 として指摘した「`generateDraft` 内 `defenseHistory` ループに `truncate` 未適用」が本タスクで修正されたことを確認。
- `generateDefenseResponse` の `defenseHistory`（L44-47）は `apiMessages` としてそのまま SDK に渡されており XML プロンプト注入対象外。このパスへの `escapeXml` / `truncate` の不適用は設計上正しい。
- `text-utils.ts` の `truncate` は `str.slice(0, max)` の単純実装。500文字でカットされた文字列が `escapeXml` に渡されるため、エスケープ処理は切り詰め後の文字列に対して行われる。副作用なし。

**判定: 合格**

---

### E-2: `app/api/cases/[id]/route.ts` — try-catch のスコープ

| チェック項目 | 結果 |
|---|---|
| GET ハンドラ（L25-48）: `createSessionClient()` と `getUser()` の両呼び出しが try-catch 内か | 合格 |
| PATCH 非 asGuest パス（L74-83）: `createSessionClient()` と `getUser()` の両呼び出しが try-catch 内か | 合格 |
| catch ブロックがスタックトレース・内部パスをレスポンスに含めないか | 合格 |
| エラーメッセージが汎用表現か | 合格 |
| try-catch 後に `if (!user)` チェックが続き、スコープが適切か | 合格 |
| asGuest パス（L127-159）の try-catch との一貫性 | 合格 |

**詳細**:

- GET ハンドラ: L25-48 の try ブロックに `createSessionClient()`（L26）と `supabase.auth.getUser()`（L27）の両呼び出しが包含されている。D-1 監査時点から変更なし・既存実装で正しい。
- PATCH 非 asGuest パス: L73 で `if (!body.asGuest)` 分岐に入った後、L74-83 の try ブロック内で `createSessionClient()`（L75）と `getUser()`（L76）を呼び出している。D-1 監査の LOW-2「createSessionClient が try-catch 外」が本タスクで解消されたことを確認。
- catch ブロック（L79-83）: `console.error("createSessionClient failed:", err)` のみ。`err` の詳細はサーバーログにのみ出力され、レスポンスには `"サーバー設定エラーが発生しました。管理者に連絡してください。"` という汎用メッセージと HTTP 500 のみ返す。スタックトレース漏洩なし。
- L85: `if (!user) return ...` は try-catch の外に配置されており、`user` 変数のスコープは L73 で `let user;` として宣言されている。catch で 500 を return した場合はここに到達しないため問題なし。
- asGuest パス: L151-158 の try-catch（generateGuestToken）も同一パターン。`console.error` のみでレスポンスは汎用メッセージ。

**判定: 合格**

---

### E-4: `lib/claude.ts` — validateApiKey のエラー種別区別

| チェック項目 | 結果 |
|---|---|
| `Anthropic` が L1 で `import Anthropic from "@anthropic-ai/sdk"` としてインポートされているか | 合格 |
| `catch (error)` として変数をバインドしているか（旧: `catch {`） | 合格 |
| `error instanceof Anthropic.AuthenticationError` で認証エラーを `false` に変換しているか | 合格 |
| `throw error` で AuthenticationError 以外を再 throw しているか | 合格 |
| 再 throw により上位ハンドラが 500 を返す設計か | 合格 |
| ネットワーク・タイムアウト等の一時的エラーが再 throw される設計か（意図通りか） | 合格 |

**詳細**:

- L1: `import Anthropic from "@anthropic-ai/sdk"` — デフォルトエクスポートの `Anthropic` クラスに `AuthenticationError` がネストされたクラスとして定義されている。`Anthropic.AuthenticationError` は SDK の公式 API であり、追加インポート不要。
- L13-16: `catch (error) { if (error instanceof Anthropic.AuthenticationError) return false; throw error; }` — 認証エラーのみ `false` を返し、それ以外（`APIConnectionError`・`APITimeoutError`・`RateLimitError` 等）は再 throw する。
- 再 throw されたエラーは呼び出し元の API ルートハンドラ（`app/api/profile/route.ts` 等）の try-catch に委譲される。当該ハンドラが 500 を返す設計であることは別ファイルの責務であり、`validateApiKey` 自体は正しい。
- ネットワークタイムアウト等の一時的エラーを再 throw する設計は意図的。呼び出し元でリトライ or 500 応答を選択できる。`validateApiKey` がすべての例外を `false` に変換すると一時的障害時に「APIキーが無効」と誤判定するリスクがあり、現設計の方が正しい。

**判定: 合格**

---

### E-6: `middleware.ts` — プレフィックスマッチ

| チェック項目 | 結果 |
|---|---|
| `pathname === "/"` の完全一致が維持されており `startsWith("/")` が使われていないか | 合格 |
| `PROTECTED_PATH_PREFIXES = ["/history", "/profile", "/case"]` が存在するか | 合格 |
| `pathname.startsWith(p + "/")` でサブルート（`/history/sub`・`/case/123` 等）が保護されるか | 合格 |
| `/api/...` が誤って保護対象に含まれていないか | 合格 |
| `config.matcher` の正規表現が `api` を除外しているか | 合格 |
| 既存の保護パス（`/`・`/history`）がすべて維持されているか | 合格 |

**詳細**:

- L32: `const PROTECTED_PATH_PREFIXES = ["/history", "/profile", "/case"]` — 保護プレフィックス一覧。`/history`・`/profile`・`/case` の3パス。
- L33-35: 判定ロジック: `pathname === "/"` の完全一致 OR `PROTECTED_PATH_PREFIXES.some(p => pathname === p || pathname.startsWith(p + "/"))` — `pathname === p` で完全一致パス（例: `/history`）を保護し、`pathname.startsWith(p + "/")` でサブルート（例: `/history/xxx`・`/case/abc-123`）を保護する。`startsWith("/")` のみの過剰マッチは発生しない。
- `config.matcher`（L44-46）: `/((?!_next/static|_next/image|favicon.ico|auth|api|.*\\.(?:svg|png|jpg|jpeg|gif|webp)$).*)` — `api` が除外パターンに明記されており、`/api/...` へのリクエストはこの middleware を通過しない。`/api/...` が保護対象に誤って含まれるリスクは排除されている。
- `/auth/...` も matcher から除外されており、ログインページへのリダイレクトループが発生しない。
- 旧実装の `startsWith("/history")` 単独パターン（`/historyx` 等の誤マッチリスク）は、`pathname === p || pathname.startsWith(p + "/")` の二段階判定で解消されている。

**判定: 合格**

---

## 新規指摘事項

なし。前回監査（D-1・D-2・D-5）で記録した LOW-1・LOW-2 の両件が本タスク（E-1・E-2）で解消されたことを確認。

---

## 総合判定

**PASS**

HIGH 0件・MEDIUM 0件・LOW 0件。E-1・E-2・E-4・E-6 の実装はすべて設計意図に合致しており、セキュリティ上の問題は検出されなかった。前回監査の指摘事項（LOW-1・LOW-2）も本タスクで完全に解消されている。
