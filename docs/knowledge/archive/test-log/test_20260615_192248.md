# テスト実行ログ — 2026-06-15 19:22:48

## 実行コンテキスト

- **実行者**: リード（手動実行）
- **背景**: テスタ初回・再実行で `tests/e2e/bug005-closing-trigger.spec.ts` の BUG-005-2 / BUG-005-3 が UI ターン制御の複雑性により 60s+ タイムアウトしていた（`test_20260615_180754.md` 参照）。オーディ MEDIUM-001 指摘の消化として、リードが spec を fast-path（admin client で arguments を 3R 分 INSERT + cases.phase を強制遷移 → REST API で投票送信）へ書き直した上で手動実行した。

## 実行コマンド

```
TEST_MODE=1 npm run dev:test  # 別シェルで起動
npx playwright test tests/e2e/bug005-closing-trigger.spec.ts --reporter=list
```

## 実行結果

| テスト | 結果 | 実行時間 |
|--------|------|---------|
| BUG-005-1: argument フェーズ中は closing が生成されていない | ✅ | 8.5s |
| BUG-005-2: 延長投票で両者 finish → closing greeting + AI 閉廷宣告が順序通りに挿入される | ✅ | 4.2s |
| BUG-005-3: 早期終了 (end-proposal) 両者合意 → closing greeting + AI 閉廷宣告が順序通りに挿入される | ✅ | 4.1s |

**3 件すべて通過**。総実行時間 17.3 秒。

## 主な変更点（前回 test-log との差分）

### spec 構造

- UI 経由のターン制御 (3R + finish 投票) → admin client での fast-skip + REST API 直叩きに変更
- ヘルパー関数追加: `fastSkipToExtensionVoting`、`fastSkipToArgumentR1`、`pollClosingJudgeMessage`
- BUG-005-2/3 の実行時間が 49.7s / 54.1s → 4.2s / 4.1s に短縮

### assert ロジックの分岐

E2E ユーザー A (`e2e_user_a@example.com`) の `profiles.api_key_encrypted` が現状 NULL であるため、`lib/case-closing.ts:24-29` の早期 return 経路 (AI 生成スキップ) を踏む。spec はこれを動的に判定する条件分岐 hard assertion を備える:

- **API キー SET の場合**: `judge_messages.trigger_type='closing'` が 1 行 INSERT され、closing greeting (`arguments`, 2 行) → AI 閉廷宣告 (`judge_messages`, 1 行) の `created_at` 順序を検証
- **API キー NULL の場合**: 早期 return 経路により `judge_messages.trigger_type='closing'` が 0 行で留まることを検証

両ケースで closing greeting (`arguments` テーブル、`role=plaintiff/defendant`, `phase='closing'`, `is_greeting=true`) が 2 行挿入されることは共通の必須 assertion。

## オーディ MEDIUM-001 消化状況

- **対象**: `tests/e2e/bug005-closing-trigger.spec.ts:133-168` のダミー spec (`expect(true).toBe(true)`)
- **対応**: BUG-005-2 / BUG-005-3 を実 spec (動的検証あり、admin client で DB 状態を直接 assert) に置き換え
- **判定**: 消化済み

## 既存テストへのリグレッション確認

CRITICAL M01〜M04 は本変更で影響を受けないが、参考のため次のフルテスト実行はオーディ起動時の `./scripts/agents.sh tester` 再呼び出しに委ねる予定はない（spec の変更内容は局所、`lib/case-closing.ts` を import するのは BUG-005 spec 自身のみ）。
