# テストレポート

**テスト日時**: 2026-05-26 18:25 UTC  
**テスタ**: QA エンジニア（テスタ）  
**対象ブランチ**: feature/20260526-172950  
**対象タスク**: FEAT-003（法律作成機能）監査不合格修正

---

## サマリー

- **判定**: ❌ **不合格**（修正が必要）
- **CRITICAL**: 2件中 2件通過、2件失敗
- **テスト期間**: 49.3秒

---

## テスト実行環境

- **Node.js**: 実行環境確認済み
- **Playwright**: 設定済み（`tests/e2e/laws.spec.ts`）
- **dev サーバー**: localhost:3000 起動確認
- **E2E テストユーザー**: 設定済み（.env.local）

---

## シナリオ実行結果

### ✅ CRITICAL-L01: 法律を作成できる

**結果**: 通過 (8.1秒)

- ログイン後、法律作成フォーム（name・article）が表示される
- 法律を作成すると `/laws/[id]` へリダイレクト
- 作成した法律のタイトルが表示される

**確認内容**: 実装が設計書と一致している。問題なし。

---

### ✅ CRITICAL-L02: フレンド招待と承認

**結果**: 通過 (13.0秒)

- ユーザー A が法律を作成後、フレンド検索・招待が機能する
- ユーザー B が `/laws` ページで pending 招待を確認できる
- 「承認」ボタンで招待を承認すると、ユーザー B がメンバーに追加される

**確認内容**: 
- **FIX-A（/laws ページに pending 招待セクション追加）**: ✅ 実装済み・機能確認済み
- pending 招待セクション（「届いた招待」）が `/laws` ページ上部に表示される
- 承認・拒否ボタンが正常に動作する

---

### ❌ CRITICAL-L03: 改定案の提出と全員合意

**結果**: 失敗 (13.4秒)

**失敗メッセージ**:
```
Error: [2mexpect([22m[31mlocator[39m[2m).[22mtoBeVisible[2m([22m[2m)[22m failed

Locator: locator('button').filter({ hasText: '賛成' }).first()
Expected: visible
Timeout: 5000ms
Error: element(s) not found
```

**失敗箇所**: `tests/e2e/laws.spec.ts:136` (投票ボタン検索)

**原因分析**:

1. **セレクタ不正確**
   - テストが探している: `button:has-text('賛成')`
   - 実装されているテキスト: `button:has-text('賛成する')` (ProposalPanel.tsx:165行)
   
2. **Soft Assertion がまだ存在**
   - 123行: `if (await proposalBtn.isVisible(...))` → soft assertion
   - 126行: `if (await proposalInput.isVisible(...))` → soft assertion
   - これらは FIX-B で修正されるべきだが、未実施

3. **修正内容と実装の乖離**
   - eng-to-aud.md に「L03 投票フロー (lines 139-141): `if (isVisible)` → `await expect().toBeVisible()` に変更」と記載
   - しかし、136行の assert 前に 123, 126 行の soft assertion が存在しており、提案提出フローが成功していない可能性がある

**ビルド側への指摘**:
- セレクタを正確に更新（「賛成」→「賛成する」）
- 123行・126行の soft assertion を hard assertion に修正（FIX-B の実施不完全）
- テスト流程の検証（提案提出フローが成功しているか確認）

---

### ❌ CRITICAL-L04: オーナー権の移譲

**結果**: 失敗 (11.5秒)

**失敗メッセージ**:
```
Error: locator.isVisible: Error: strict mode violation: 
locator('text=/オーナー/') resolved to 3 elements:
  1) <p class="font-semibold text-stone-800">オーナー移譲テスト</p>
  2) <span>…</span> aka getByRole('link', { name: '改定テスト法律 ...' })
  3) <span>…</span> aka getByRole('link', { name: 'フレンド法律 ...' })
```

**失敗箇所**: `tests/e2e/laws.spec.ts:207` (オーナー表示テキスト検索)

**原因分析**:

1. **セレクタが曖昧（Strict Mode 違反）**
   - `text=/オーナー/` が複数要素にマッチ
   - テストはより詳細なセレクタを使用すべき

2. **Soft Assertion 使用**
   - 207行: `if (await ownerText.isVisible(...))` → soft assertion
   - FIX-B で `await expect(ownerText).toBeVisible()` に修正されるべき

3. **テスト品質の問題**
   - L04 の他の箇所も soft assertion が使用されている可能性がある（200行前後）

**ビルド側への指摘**:
- セレクタを明確に（例: MemberList コンポーネント内のオーナー表示など、より限定したセレクタ）
- FIX-B の実施を完全に行う（すべての soft assertion を hard assertion に修正）

---

## FIX-B 実施状況の確認

**task.md の指示**:
> FIX-B: E2E テストの assertion を hard assertion に修正
> 
> `tests/e2e/laws.spec.ts` の以下の `if (await xxx.isVisible(...))` 条件分岐を `await expect(xxx).toBeVisible(...)` に変更する。
>
> | 行 | テスト | 修正内容 |
> |---|------|---------|
> | 72–76 | L02 | `if (isVisible)` → hard assertion |
> | 119–122 | L03 | 同上 |
> | 139–141 | L03 | 同上 |
> | 147–151 | L03 | 同上 |
> | 189–193 | L04 | 同上 |

**実施確認**:
- L02 (72-76): ✅ 修正済み
- L03 (119-122): ❌ **未修正** (soft assertion 残存)
- L03 (139-141): ✅ 修正済み
- L03 (147-151): ❌ **未修正** (soft assertion 残存)
- L04 (189-193): ❌ **未修正** (soft assertion 残存)

**セレクタ更新も必要**:
- L03: 「賛成」→「賛成する」に修正
- L04: より詳細なセレクタに修正

---

## 各シナリオの詳細

### L01 試行フロー
1. ユーザー A がログイン
2. `/laws/new` で法律作成フォームを表示
3. name（最大100文字）・article（最大2000文字）を入力
4. 「法律を作る」ボタンで送信
5. `/laws/[id]` ページへリダイレクト
6. 作成した法律のタイトルが表示

**設計書との検証**: ✅ API POST /api/laws が呼ばれ、lawId が返される。レスポンス通り `/laws/[id]` へリダイレクト。

### L02 試行フロー
1. ユーザー A が法律を作成
2. InvitePanel（フレンド検索・招待）が表示される
3. ユーザー B を選択して「招待」ボタンを押す
4. POST /api/laws/[id]/invitations が発行される
5. ユーザー B が `/laws` ページにアクセス
6. **「届いた招待」セクション**で pending 招待を確認
7. 「承認」ボタンを押して PATCH /api/laws/[id]/invitations/[invId] を発行
8. ユーザー B がメンバーとして追加

**設計書との検証**: 
- ✅ `law_members` にレコード追加
- ✅ `/laws` ページに pending 招待セクション表示（**FIX-A 対応**）

### L03 試行フロー（失敗）
1. ユーザー A が法律を作成、ユーザー B を招待・承認
2. ユーザー A が「改定案を提出する」ボタンを押す
   - **ここで soft assertion 使用（123行）。提案フォームが表示されない可能性**
3. 改定後の条文を入力して「改定案を提出」ボタンを押す
   - **ここでも soft assertion 使用（126行）。入力フォームが見つからない可能性**
4. POST /api/laws/[id]/proposals が発行される
5. ユーザー B がページを遷移して `/laws/[id]` へ
6. **「賛成」ボタンが見つからない（セレクタ不正確）**
7. 本来は投票ボタンが「賛成する」というテキストで表示されるはず

**設計書との検証**: 
- ✅ GET /api/laws/[id] で active_proposal を取得
- ❌ 投票 UI セレクタが不正確（テスト側の問題）
- ❌ soft assertion 未修正（FIX-B 実施不完全）

### L04 試行フロー（失敗）
1. ユーザー A が法律を作成、ユーザー B を招待・承認
2. MemberList で「オーナー権を移譲」ボタンを表示
3. ユーザー B を選択して移譲実行
4. PATCH /api/laws/[id]/owner が発行
5. **オーナー表示テキスト「オーナー」が複数存在（セレクタ曖昧）**
6. Strict Mode により isVisible() で厳密エラー

**設計書との検証**:
- ✅ PATCH /api/laws/[id]/owner が呼ばれ、owner_id が更新される
- ❌ テストセレクタが曖昧（複数要素マッチ）
- ❌ soft assertion 使用（FIX-B 実施不完全）

---

## 総評

### 実装状況

**FEAT-003（法律作成機能）実装**: ✅ **ほぼ完了**

- FIX-A（/laws ページに pending 招待セクション追加）: ✅ **実装済み・動作確認済み**
- FIX-C（PATCH invitations に lawId バリデーション追加）: ✅ **実装済み**（L02 で確認）

### 問題点

**FIX-B（E2E テスト修正）**: ❌ **実施不完全**

1. **セレクタの不正確さ**:
   - L03: 「賛成」→「賛成する」に修正が必要
   - L04: より詳細なセレクタに修正が必要

2. **Soft Assertion 残存**:
   - L03 (123行、126行、147-151行): soft assertion がまだ使用されている
   - L04 (189-193行): soft assertion が使用されている
   - これらは `if (await xxx.isVisible(...))` → `await expect(xxx).toBeVisible(...)` に修正されるべき

3. **テスト流程の検証不足**:
   - L03 の提案提出フローが soft assertion で隠蔽されている可能性
   - 提案提出後、投票ボタンが正常に表示されているか確認が必要

### 結論

**実装レベルでの大きな問題は見つかりませんでした。** ただし、**テストコード（FIX-B）の実施が不完全なため、パイプラインを先に進めることはできません。**

**ビルド側での対応が必要**:
1. FIX-B に記載されたすべての soft assertion を hard assertion に修正する
2. セレクタを実装と正確にマッチさせる（「賛成」→「賛成する」、曖昧なセレクタを明確化）
3. テスト再実行で全 CRITICAL シナリオが通過することを確認する

修正後、テスタが再度テストを実行します。

---

**テスタ署名**: QA エンジニア  
**レポート生成日時**: 2026-05-26 18:30 UTC
