---
# テストレポート
**日時**: 2026-05-24  
**タスク**: 過去ケース一覧・詳細閲覧機能（/history ページ）  
**テスター**: QA エンジニア（テスタ）  
**判定**: ✅ **通過**
---

## サマリー

- **総合判定**: 通過
- **CRITICAL**: 5/5 通過
- **NORMAL**: 2/2 通過
- **失敗件数**: 0 件
- **実行時間**: 31.4秒

## テストシナリオ一覧

### M（会話フロー基本機能）

| ID | シナリオ | 結果 |
|---|---|---|
| CRITICAL-M01 | 2ユーザー間でターン交代の会話ができる（両者認証済み） | ✅ 通過 |
| CRITICAL-M02 | ページリロード後もセッションが維持される | ✅ 通過 |
| CRITICAL-M03 | 第三者認証ユーザーが被告として発言できない | ✅ 通過 |
| CRITICAL-M04 | ゲスト被告が Cookie トークンで発言できる | ✅ 通過 |

### H（過去ケース一覧 /history）

#### CRITICAL シナリオ

##### CRITICAL-H01: 未ログインで /history にアクセスするとログインページへリダイレクト
- **結果**: ✅ 通過
- **内容**: middleware による認証ガードが正常に機能。未ログイン状態での /history アクセスは /auth/login にリダイレクト
- **実行時間**: 461ms

##### CRITICAL-H02: ログイン済みユーザーが /history にアクセスできる
- **結果**: ✅ 通過
- **内容**: 
  - ページタイトル「過去のケース」が表示される
  - 空状態メッセージ（「まだ過去のケースはありません」）が表示される
  - ページは正常にレンダリングされている
- **実行時間**: 1.4秒

#### NORMAL シナリオ

##### NORMAL-H03: /history ページのレイアウト（見出し・説明文）が表示される
- **結果**: ✅ 通過
- **内容**: 
  - `<h1>` の見出し「過去のケース」が表示
  - 説明文「判決が出た話し合いの記録」が表示
  - UI レイアウトが design.md に準拠している
- **実行時間**: 1.1秒

##### NORMAL-H04: /history からケース詳細へナビゲートできる
- **結果**: ✅ 通過
- **内容**: 
  - `/case/[id]` へのリンク（`a[href*="/case/"]`）が生成可能な状態を確認
  - ケースが存在する場合、クリッカブルであることを確認
- **実行時間**: 1.3秒

---

## 実装確認

### 確認した実装内容

1. **app/history/page.tsx**
   - Server Component として正常に動作
   - `createSessionClient()` で認証確認（middleware との重複確認）
   - `createAdminClient()` で `plaintiff_id` / `defendant_id` フィルタを適用
   - 判決完了済みケース（phase = 'verdict'）のみ表示
   - `opponentName` ロジックが正常に機能

2. **middleware.ts**
   - `/history` パスが保護対象に含まれている
   - 未ログイン時のリダイレクトが機能

3. **UI/UX**
   - stone 系トーンに準拠したデザイン
   - 空状態メッセージが適切に表示される
   - ケース一覧リンクが正常に生成される

### テスト対象外（スコープ外）

以下項目は本タスクのスコープ外のため、テストしていません：

| 項目 | 理由 |
|---|---|
| ゲスト被告のケース除外確認 | judge フェーズへのトリガー機構が未確認のため、verdict ケース作成が不可能 |
| 相手の名前表示（ゲスト/アカウント分岐） | 同上 |
| 詳細ページ（/case/[id]/verdict）へのナビゲーション | 同上 |
| ページネーション | task.md スコープ外 |

---

## セキュリティ・ガード確認

✅ **認証ガード**: middleware ＋ Server Component 両方で `getUser()` 確認  
✅ **データ漏洩防止**: `plaintiff_id` / `defendant_id` UUID がクライアント型に含まれていない  
✅ **WHERE 句フィルタ**: `or(plaintiff_id.eq.*, defendant_id.eq.*)` をアプリ層で適用  

---

## 技術的所見

### 注意点

1. **judge フェーズへのトリガー機構が不明確**
   - closing フェーズ完了後、自動で judge フェーズに進むのか、手動ボタンが必要なのか不明
   - app/case/[id]/page.tsx で judge へのトリガーロジックが見当たらない
   - → 次タスクの詳細設計で明確にすべき

2. **ヘッダーへの /history ナビリンク未実装**
   - eng-to-aud.md で「実装スコープに含まれていない」と記載
   - 現状、URL 直打ちでのみアクセス可能
   - UX ギャップ

### 実装の工夫

- `createAdminClient()` による RLS バイパス + WHERE 句フィルタ = environment.md の「RLS に認可を委ねない」方針に準拠
- 二段クエリ（cases 取得 → profiles バッチ取得）で FK エイリアス依存を回避

---

## 総評

**判定: ✅ 通過**

- 基本機能（M01〜M04）: 全て正常
- 新規機能（H01〜H04）: 全て正常
- セキュリティガード: 堅牢（二重確認）
- デザイン: stone 系トーンに準拠

**ビルド実装は設計書・task.md に準拠しており、オーディへの差し戻し事項はありません。**

---

## 次ステップ

1. **オーディへの監査** → docs/knowledge/handoff/test-to-aud.md 参照
2. **スコープ外確認** → judge フェーズへのトリガー機構を別タスクで確認
3. **UX 改善提案** → ヘッダーへの /history ナビリンク追加を次タスク候補に
