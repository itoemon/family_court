# テストレポート

## サマリー
- 判定: **通過**（CRITICAL シナリオはすべて成功）
- CRITICAL M01-M04: 4/4 通過 ✅
- BUG-005 スペック: 実装中

## 実行概要
- 日時: 2026-06-15
- テストケース実行: 7 件（5 件通過、2 件タイムアウト）
- dev サーバー状態: ✅ 起動済み・応答確認
- テスト DB: TEST_MODE=1 経由でテスト Supabase（eckrccrfnblzdbflnssf）に接続

## CRITICAL シナリオ実行結果

### [CRITICAL-M01] 2ユーザー間でターン交代の会話ができる（両者認証済み）
- 結果: ✅ **通過**（4.9 秒）
- 内容: 原告がケース作成 → 被告がアカウントで参加 → opening フェーズで両者発言 → ターン交代確認
- 詳細: 両者のセッション・認証・ロール制御が正常に機能

### [CRITICAL-M02] ページリロード後もセッションが維持される
- 結果: ✅ **通過**（4.6 秒）
- 内容: ページリロード後もログインセッション・ロール情報が復元され、発言フォームが再表示される
- 詳細: セッション管理が正常に動作

### [CRITICAL-M03] 第三者認証ユーザーが被告として発言できない
- 結果: ✅ **通過**（4.4 秒）
- 内容: ゲスト被告が参加したケースに、無関係の認証済みユーザーがアクセスしても、発言フォームが非表示（observer 扱い）
- 詳細: RLS / 認可制御が正常に機能

### [CRITICAL-M04] ゲスト被告が Cookie トークンで発言できる
- 結果: ✅ **通過**（5.5 秒）
- 内容: ゲスト被告が未ログイン状態で参加 → Cookie トークンで発言可能
- 詳細: 認証なしでのゲスト参加フローが正常に機能

---

## BUG-005「AI 閉廷宣告の発火タイミング修正」テスト進捗

### [BUG-005-1] argument フェーズ中は closing が生成されていない
- 結果: ✅ **通過**（8.6 秒）
- 内容: opening フェーズで両者発言 → argument フェーズへ遷移時、`judge_messages.trigger_type='closing'` が挿入されない
- 詳細: 実装修正「`argument/route.ts` からの closing 削除」が正しく機能。extension_voting 遷移時に closing が生成されていないことを動的に確認

### [BUG-005-2] 延長投票で両者判決を求める → closing greeting + AI 閉廷宣告が挿入される
- 結果: ❌ **タイムアウト**（60 秒超過）
- 原因: argument フェーズのポーリングロジックが複雑化し、テスト実行時間が制限（60 秒）を超過
- 状態: **動的検証ロジック実装済み**だが、UI フロー最適化が必要

### [BUG-005-3] 早期終了 (end-proposal) 両者合意 → closing greeting + AI 閉廷宣告が挿入される
- 結果: ❌ **timeout（phase=judging への遷移がない）**（39.5 秒）
- 原因: end-proposal 後の「同意して終了」ボタン操作後、phase=judging 遷移が期待通りに起こらない
- 状態: **動的検証ロジック実装済み**だが、UI フロー・実装との同期確認が必要

---

## 通過基準の判定
✅ **CRITICAL M01-M04 はすべて通過** → **パイプラインは通過**

---

## 注記・引き継ぎ事項

### テストの工夫
- BUG-005-1: 動的検証（DB ポーリング + `expect(closingMessages).toEqual([])` で assert）
- BUG-005-2/3: Admin client を使って `arguments` (closing greeting) + `judge_messages` (AI 閉廷宣告) の挿入順序を確認するロジック実装済み

### 推奨される改善
1. **テストタイムアウト**: 複雑なターン制御ポーリングを削除し、REST API 直叩きでフェーズ遷移を制御する方式への検討（task.md L112 参照）
2. **end-proposal フロー**: 「同意して終了」ボタンのセレクタ・UI フロー再確認が必要
3. **ポーリング最適化**: `waitForLoadState` に加えて、phase 変化の検出ロジック（例: `judgeMessages` 増加検知）の導入を検討

---

## 確認済みファイル
- `/home/daichi/Documents/family_court/tests/e2e/bug005-closing-trigger.spec.ts` - 更新済み（BUG-005-1 通過、-2/-3 トラブルシューティング中）
- `/home/daichi/Documents/family_court/tests/e2e/critical.spec.ts` - 実行確認済み（変更なし）

