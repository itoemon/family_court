---
# 監査レポート

**対象タスク**: セキュリティ MEDIUM 3件・パフォーマンス MEDIUM 2件の一括修正  
**対象ブランチ**: feature/20260525-093502  
**実施日**: 2026-05-25  

## サマリー
- 指摘件数: HIGH 0件 / MEDIUM 0件 / LOW 1件

---

## 実装確認結果

### A-1: `lib/guest-token.ts` — fail-fast をモジュールトップレベルへ移動

IIFE で `const GUEST_TOKEN_SECRET: string` として確定させている。`computeToken` 内の冗長なチェックも削除済み。設計書の After コードと完全一致。✅

### A-2: `lib/judge.ts` — プロンプトインジェクション対策

`buildPrompt` 冒頭で `truncate(X, 50) → escapeXml` の順で前処理し、各 trigger（opening / closing / turn）の全プロンプトを XML タグ囲み + 注意書きに変換している。`escapeXml` は `& < > " '` の 5 種を正しい順（`&` を最初）に処理しており、`&lt;` の二重エスケープは発生しない。`safeLastSpeakerName` / `safeNextSpeakerName` を `safePlaintiff` / `safeDefendant` から派生させている。設計書と完全一致。✅

### A-3: `app/api/cases/[id]/route.ts` — ゲスト被告名の最大長バリデーション

空チェック直後（107–109行）に `body.defendantName.trim().length > 50` チェックを追加。超過時は `{ error: "名前は50文字以内で入力してください" }` + 400 を返す。認証済み被告フロー（`asGuest: false`）は変更なし。設計書と完全一致。✅

### C-1: `app/api/cases/[id]/argument/route.ts` — profiles クエリ重複解消

110行目コメント「profiles は judge・矛盾チェック両方で使うため先に1回取得」に示されるとおり、111–118行で `display_name` と `api_key_encrypted` を1回のクエリで同時取得し、judge 生成・矛盾チェックの両方で使い回している。✅

### C-2: `lib/case-response.ts` — contradiction_warnings クエリ件数上限

56行に `.limit(100)` が存在することを確認。✅

### バックログ MEDIUM-001: `defense/route.ts`・`draft/route.ts` の try-catch 追加

前回監査（audit_20260525_092649.md）の指摘事項。`verifyGuestToken` 呼び出しを含む認証ブロック全体を try-catch で囲み、既存の `argument/route.ts` と同様のエラーレスポンス（500）を返す形に修正されている。✅

---

## 指摘一覧

### [LOW-001] A-2 テストで `E2E_TEST_EMAIL_B`・`E2E_TEST_PASSWORD_B` が必須チェックから漏れている（`tests/e2e/security-fixes.spec.ts`:7–13, 121–123）

- **内容**:  
  `beforeEach` の必須環境変数チェックに `E2E_TEST_EMAIL_A`・`E2E_TEST_PASSWORD_A` のみを指定している（7–13行）。一方、A-2 テスト「特殊文字を含む名前でも judge メッセージが破綻しない」は `E2E_TEST_EMAIL_B`・`E2E_TEST_PASSWORD_B` を `process.env` から直接参照している（121–123行）。  
  これらが未設定の CI 環境では、テストは skip されずに `loginAs(pageB, undefined, undefined)` が呼ばれてランタイムエラーで失敗する。エラーメッセージからは「環境変数が足りない」と診断しにくい。

- **修正案**:  
  `beforeEach` の `required` 配列に A-2 テストが使用する変数を追加する。または A-2 テスト冒頭で個別にチェックし、未設定時に `test.skip()` を呼ぶ。

  ```ts
  // パターン①: beforeEach を修正
  const required = [
    'E2E_TEST_EMAIL_A', 'E2E_TEST_PASSWORD_A',
    'E2E_TEST_EMAIL_B', 'E2E_TEST_PASSWORD_B',
  ];

  // パターン②: A-2 テスト内で個別チェック
  test('A-2: ...', async ({ browser }) => {
    const emailB = process.env.E2E_TEST_EMAIL_B;
    const passB  = process.env.E2E_TEST_PASSWORD_B;
    if (!emailB || !passB) test.skip(true, 'E2E_TEST_EMAIL_B / PASSWORD_B が未設定');
    // ...
  });
  ```

---

## 総評

A-1・A-2・A-3 いずれも設計書の仕様に忠実に実装されており、防御の多層構造（境界バリデーション → XML タグ構造分離 → エスケープ → truncate）が正しく機能している。C-1・C-2 の実装済み確認も問題なし。前回バックログ MEDIUM-001（verifyGuestToken の未処理例外）の try-catch 修正も既存パターンと整合している。

**判定: 通過**（HIGH 0件、指摘総数 1件 ≤ 5件）
