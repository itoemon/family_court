# 監査ログ: 2026-05-25 17:30

## 対象ブランチ
feature/20260525-131143

## 監査結果サマリー

| 重要度 | 件数 |
|--------|------|
| HIGH   | 0    |
| MEDIUM | 0    |
| LOW    | 2    |
| 合格   | 8    |

## 各項目の確認結果

---

### D-1: `lib/defense.ts` — truncate の適用

| チェック項目 | 結果 |
|---|---|
| `truncate` → `escapeXml` の順序（48行目・82行目） | 合格 |
| `generateDefenseResponse` に適用済み | 合格 |
| `generateDraft` に適用済み | 合格 |
| `lib/judge.ts` の `truncate` が `export function` になっているか | 合格 |
| `defenseHistory` の content は変更なし（route.ts で 1000 文字バリデーション済みのため対象外） | 合格 |

**詳細**:

- `defense.ts` 48行目: `escapeXml(truncate(a.content, 500))` — truncate が先に適用され、その出力を escapeXml が処理する。順序正しい。
- `defense.ts` 82行目: 同じパターンで `generateDraft` にも適用済み。
- `judge.ts` 4行目: `export function truncate(str: string, max: number): string` — named export に変更済み。
- `topic` フィールドには `truncate` が適用されていないが、コメント（judge.ts 45行目）に「topic は保存前に 200 文字バリデーション済み」と明記されており、設計上意図的な除外。

**観察（LOW）**:

- `generateDraft`（defense.ts 88行目）内の `defenseHistory` ループで `escapeXml(m.content)` が適用されているが、`truncate` は適用されていない。`defenseHistory` の内容はユーザー入力（route.ts で 1000 文字バリデーション済み）と AI 応答（長さ無制限）が混在する。AI 応答は claude-haiku の `max_tokens: 512` で上限されるため実害は低いが、防御的観点では `truncate` の適用が望ましい。→ LOW-1 として記録。

**判定: 合格**

---

### D-2: `defense/route.ts` — try-catch のスコープ

| チェック項目 | 結果 |
|---|---|
| 認証ユーザーパスが try-catch で囲まれているか（resolveAuth 15〜30行目） | 合格 |
| catch ブロックにスタックトレース・内部パスが含まれないか | 合格 |
| エラーメッセージが汎用表現か | 合格 |
| ゲストパスの try-catch との一貫性 | 合格 |

**詳細**:

- `resolveAuth` の認証ユーザーパス（15〜30行目）: `createSessionClient()` 呼び出しから `user` 判定ブロック末尾まで try-catch で囲まれている。catch ブロックは `console.error("createSessionClient failed:", err)` のみで、レスポンスには `"サーバー設定エラーが発生しました。管理者に連絡してください。"` という汎用メッセージを返す。スタックトレース漏洩なし。
- ゲストパスの try-catch（33〜41行目）も同一パターン。`console.error("verifyGuestToken failed:", err)` のみでレスポンスは汎用メッセージ。
- `argument/route.ts`（既存実装）の try-catch パターンと一貫している。

**判定: 合格**

---

### D-5: 空文字列ガード（3箇所）

| チェック項目 | 結果 |
|---|---|
| `route.ts` アカウント参加時 opening（96〜98行目）の `if (content)` | 合格 |
| `route.ts` ゲスト参加時 opening（132〜134行目）の `if (content)` | 合格 |
| `argument/route.ts` turn/closing（143〜145行目）の `if (content)` | 合格 |
| `if (content)` で空文字列・null・undefined を正しくガードできるか | 合格 |
| INSERT スキップによる UX への副作用なし | 合格 |

**詳細**:

- 3箇所すべてに `if (content) { await admin.from("judge_messages").insert(...) }` が実装済み。
- `generateJudgeMessage` の返却値は `string` 型（空文字列の場合は `""`）。`if (content)` は JavaScript のトゥルーシーネスにより空文字列・null・undefined をすべて除外する。正しいガード。
- INSERT をスキップした場合、judge_messages レコードが作成されないだけで case の状態（phase・turn）には影響しない。開廷宣言メッセージが表示されないことはあり得るが、話し合い自体は継続できる。UX への影響は軽微かつ許容範囲内。

**観察（LOW）**:

- `route.ts` PATCH ハンドラ（非 asGuest パス、77〜106行目）: `createSessionClient()` の呼び出しが try-catch の外（72行目）にある。ただし同一関数内のゲストパスとは異なるフロー分岐であり、throw した場合は Next.js のエラーハンドラに委譲される。直接的なセキュリティリスクではないが、C-1 対応時に修正済みのパターンと非一貫。→ LOW-2 として記録。

**判定: 合格**

---

## 新規指摘事項

| ID | 重要度 | 対象ファイル | 内容 |
|----|--------|------|------|
| LOW-1 | LOW | `lib/defense.ts` | `generateDraft` 内の `defenseHistory` ループ（88行目）で `escapeXml(m.content)` に `truncate` 未適用。AI 応答は max_tokens:512 で実害は低い。次回パイプラインで防御的修正を検討。 |
| LOW-2 | LOW | `app/api/cases/[id]/route.ts` | PATCH ハンドラの非 asGuest パスで `createSessionClient()` が try-catch 外（72行目）。throw 時は Next.js のデフォルトエラーハンドラに委譲されるが、C-1 修正済みパターンと非一貫。直接的なセキュリティリスクは低い。 |

---

## 総合判定

**PASS**

HIGH 0件・指摘 2件（いずれも LOW）。D-1・D-2・D-5 の実装はすべて設計書通りかつセキュリティ上問題なし。新規指摘 2件は次回パイプラインでの対応を推奨。
